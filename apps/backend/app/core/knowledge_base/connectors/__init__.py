"""Knowledge-base source connectors.

Each connector ingests documents from an external system and produces
``DocumentSectionInput`` objects compatible with the existing chunking
pipeline in ``document_ingestion.py``.
"""
